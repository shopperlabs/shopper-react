const rules = {
  name: [
    { required: true, message: 'Please name is required', trigger: 'blur' }
  ],
  dimension: [
    { required: true, message: 'Please dimension is required', trigger: 'blur' }
  ],
}

export default rules
