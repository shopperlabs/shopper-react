const rules = {
  code: [
    { required: true, message: 'Please code is required', trigger: 'blur' }
  ],
  title: [
    { required: true, message: 'Please title is required', trigger: 'blur' }
  ],
}

export default rules
